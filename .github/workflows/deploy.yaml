name: Deploy

on:
  workflow_dispatch:
    inputs:
      gitRef:
        description: 'Commit, tag or branch name to deploy'
        required: true
        type: string
        default: 'main'
      environment:
        description: 'Environment to deploy to'
        required: true
        type: choice
        options:
        - integration
        - staging
        - production
        default: 'integration'
  push:
    branches:
      - main
    paths-ignore:
      - "Jenkinsfile"
      - ".git**"

jobs:
  trigger-deploy:
    name: Trigger deploy to ${{ github.event.inputs.environment }}
    permissions:
      deployments: write
    uses: alphagov/govuk-infrastructure/.github/workflows/deploy.yaml@deploy-via-github-action
    with:
      imageTag: ${{ inputs.gitRef }}
      workflowTrigger: ${{ github.event_name }}
      environment: ${{ github.event.inputs.environment }}
    secrets:
      GOVUK_CI_GITHUB_API_TOKEN: ${{ secrets.GOVUK_CI_GITHUB_API_TOKEN }}
