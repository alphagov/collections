<% inverse ||= false %>

<%= render 'govuk_publishing_components/components/breadcrumbs', {
  breadcrumbs: @breadcrumbs || Breadcrumbs.new(@content_item).breadcrumbs,
  collapse_on_mobile: true,
  inverse: inverse
} %>
