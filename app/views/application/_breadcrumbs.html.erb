<% inverse ||= false %>

<% breadcrumbs = defined?(@header.breadcrumbs) ? @header.breadcrumbs : Breadcrumbs.new(@content_item).breadcrumbs  %>

<%= render 'govuk_publishing_components/components/breadcrumbs', {
  breadcrumbs: breadcrumbs,
  collapse_on_mobile: true,
  inverse: inverse
} %>
