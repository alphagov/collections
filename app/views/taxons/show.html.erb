<%= render partial: 'page_header', locals: {
  taxon: taxon,
  parent_taxon: parent_taxon
} %>

<%= render partial: 'child_taxons', locals: { child_taxons: child_taxons } %>

<%= render partial: 'taxon_tagged_content', locals: {
  taxon: taxon,
  tagged_content: tagged_content
} %>
