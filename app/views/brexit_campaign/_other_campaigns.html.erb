<div class="gem-c-related-navigation" style="padding-top: 20px">
    <h2 class="campaign__other-campaigns-title">
      <%= @campaign_links[:section_title] %>
    </h2>

    <ul class="campaign__other-campaigns-list">
      <% @campaign_links[:contents_list_links].each do |link| %>
        <li class="campaign__other-campaigns-item"><a href="<%= link[:href] %>"><%= link[:text] %></a></li>
      <% end %>
    </ul>
</div>
