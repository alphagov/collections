<%= content_for :title, 'Prepare for the UK leaving the EU' %>

<% content_for :meta_tags do %>
  <%= tag("meta", name: "description", content: @campaign.description) if @campaign.description %>
<% end %>

<%= render partial: 'header' %>

<div class="taxon-page__email-link-wrapper">
  <div class="full-page-width-wrapper">
      <%= render "components/email-link", {
        text: t('campaign.email_signup'),
        href: "https://www.gov.uk/email-signup/?topic=/government/brexit",
        data: {
          module: "track-click",
          track_category: "emailAlertLinkClicked",
          track_action: "/government/brexit"
        }
      } %>
  </div>
</div>

<div class="full-page-width-wrapper">
  <%= render partial: 'all_taxons' %>
</div>

<div class="full-page-width-wrapper">
  <%= render partial: 'other_campaigns' %>
</div>
